# Distribution edge: beta with extreme parameters
# alpha=0.5, beta=0.5 → U-shaped (bimodal at edges)
# alpha=100, beta=100 → highly concentrated at midpoint
node: "Beta extreme parameters"
prior: 0.50
children:
  - node: "U-shaped beta(0.5, 0.5)"
    lr_min: 1.0
    lr_max: 4.0
    lr_dist: beta
    lr_alpha: 0.5
    lr_beta: 0.5
    evidence_type: for
  - node: "Concentrated beta(100, 100)"
    lr_min: 0.1
    lr_max: 0.5
    lr_dist: beta
    lr_alpha: 100
    lr_beta: 100
    evidence_type: against
